left_join(
id_df,
join_by(ticker)
)
X_imp = imputed_df[, 1:53] |>
dplyr::select(-c(ticker, date_q, price, price_tm1))
model_cv = cv.glmnet(x = model.matrix(~ -1 + ., X_imp),
y = log(imputed_df[['price']]),
alpha = 0, lambda = lambda,
standardize = TRUE,
intercept = TRUE)
plot(model_cv)
model_cv$lambda.min
model_cv$cvm %>%  min
model = glmnet(x = model.matrix(~ -1 + ., X_imp),
y = log(imputed_df[['price']]),
alpha = 0, lambda = model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE,)
model$beta
# adaptive lasso model
lasso_model_cv = cv.glmnet(x = model.matrix(~ -1 + ., X_imp),
y = log(imputed_df[['price']]),
alpha = 1, lambda = lambda,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
plot(lasso_model_cv)
lasso_model_cv$lambda.min
lasso_model_cv$cvm %>%  min
lasso_model_imputed = glmnet(x = model.matrix(~ -1 + ., X_imp),
y = log(imputed_df[['price']]),
alpha = 1, lambda = lasso_model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
lasso_model_imputed$beta
pred_imp = predict(
lasso_model_imputed,
model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames))
)
dim(model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames)))
length(X_imp %>%  colnames)
?model.frame
?model.matrix
# check the the two model on test data
df_test = readr::read_csv("test_with_feats.csv")
df_test = df_test |>
distinct() |>
dplyr::select(colnames(df_nona))
df_test$finnhubIndustry = ifelse(is.na(df_test$finnhubIndustry), 'other', df_test$finnhubIndustry)
pred_no_imp = predict(
lasso_model,
model.matrix(~ -1 + ., df_test |> dplyr::select(X %>%  colnames))
)
pred_imp = predict(
lasso_model_imputed,
model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames))
)
dim(model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames)))
# check the the two model on test data
df_test = readr::read_csv("test_with_feats.csv")
dim(df_test)
df_test = df_test |>
distinct() |>
dplyr::select(colnames(df_nona))
df_test$finnhubIndustry = ifelse(is.na(df_test$finnhubIndustry), 'other', df_test$finnhubIndustry)
dim(df_test)
# check the the two model on test data
df_test = readr::read_csv("test_with_feats.csv")
df_test
date_test |> arrange(ticker, date_q)
df_test |> arrange(ticker, date_q)
colnames(df_nona)
df_test = df_test |>
distinct() |>
dplyr::select(colnames(df_nona))
df_test |> arrange(ticker, date_q)
df_test$finnhubIndustry = ifelse(is.na(df_test$finnhubIndustry), 'other', df_test$finnhubIndustry)
dim(model.matrix(model.matrix(~ ., X_imp), df_test)))
dim(model.matrix(model.matrix(~ ., X_imp), df_test))
dim(model.frame(model.matrix(~ ., X_imp), df_test))
model.matrix(~ ., X_imp)
lasso_model_imputed$beta
model.matrix(~ -1 + ., X_imp)
levels(X_imp$finnhubIndustry)
X_imp$finnhubIndustry %>%  unique %>%  length
df_test$finnhubIndustry %>%  unique %>%  length
df_test |> dplyr::select(X_imp %>%  colnames)
model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames))
dim(model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames)))
colnames(model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames)))
cbind(colnames(model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames))), colnames(model.matrix(~-1+., X_imp)))
colnames(model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames)))
A = colnames(model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames)))
B = colnames(model.matrix(~-1+., X_imp))
A %in% B
B %in% A
B[35]
B[60]
# check the the two model on test data
df_test = readr::read_csv("test_with_feats.csv")
df_test = df_test |>
distinct() |>
dplyr::select(colnames(df_nona))
df_test$finnhubIndustry = ifelse(is.na(df_test$finnhubIndustry), 'other', df_test$finnhubIndustry)
df_test = df_test %>%  na.omit
df_test$finnhubIndustry = ifelse(is.na(df_test$finnhubIndustry), 'other', df_test$finnhubIndustry)
df_test = df_test %>%  na.omit
pred_no_imp = predict(
lasso_model,
model.matrix(~ -1 + ., df_test |> dplyr::select(X %>%  colnames))
)
df_test$finnhubIndustry <- factor(df_test$finnhubIndustry, levels = levels(X_imp$finnhubIndustry))
pred_imp = predict(
lasso_model_imputed,
model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames))
)
A = colnames(model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames)))
levels(X_imp$finnhubIndustry)
X_imp$finnhubIndustry
# check the the two model on test data
df_test = readr::read_csv("test_with_feats.csv")
df_test = df_test |>
distinct() |>
dplyr::select(colnames(df_nona))
df_test$finnhubIndustry = ifelse(is.na(df_test$finnhubIndustry), 'other', df_test$finnhubIndustry)
df_test = df_test %>%  na.omit
pred_no_imp = predict(
lasso_model,
model.matrix(~ -1 + ., df_test |> dplyr::select(X %>%  colnames))
)
df_test$finnhubIndustry <- factor(df_test$finnhubIndustry, levels = unique(X_imp$finnhubIndustry))
pred_imp = predict(
lasso_model_imputed,
model.matrix(~ -1 + ., df_test |> dplyr::select(X_imp %>%  colnames))
)
pred_imp
# calculate mse
mse_imp = mean((log(df_test$price) - pred_imp)**2)
mse_no_imp = mean((log(df_test$price) - pred_no_imp)**2)
mse_imp
mse_no_imp
sqrt(12.82166)
mae_imp = mean(abs(log(df_test$price) - pred_imp))
mae_no_imp = mean(abs(log(df_test$price) - pred_no_imp))
mae_imp
mae_no_imp
# mse on original scale
mse_imp_orig = mean((df_test$price - exp(pred_imp))**2)
mse_no_imp_orig = mean((df_test$price - exp(pred_no_imp))**2)
mse_imp_orig
mse_no_imp_orig
# mae on original scale
mse_imp_orig = mean(abs(df_test$price - exp(pred_imp)))
mse_no_imp_orig = mean(abs(df_test$price - exp(pred_no_imp)))
mse_imp_orig
mse_no_imp_orig
df_test$price - exp(pred_no_imp)
cbind(df_test$price, exp(pred_no_imp))
cbind(log(df_test$price), pred_no_imp)
df_test[500, ]
df |> filter(ticker == 'RVP')
cbind(df_test$price, exp(pred_no_imp))
df_test[499, ]
summary(lm(is.na(ebitPerShare) ~ finhubindustry, df))
summary(lm(is.na(ebitPerShare) ~ finnhubIndustry, df))
sect = c('Banking', 'Biotechnology', "Packaging", 'other')
mm = lm(price ~ .,
data = df_nona |> filter(!(finnhubIndustry %in% sect)))
X <- model.matrix(mm)
P <- X %*% solve(t(X) %*% X) %*% t(X)
solve(t(X) %*% X)
t(X) %*% X
X
mm = lm(price ~ ., data = df_nona |>
dplyr::select(-c(ticker, date_q)) |>
filter(!(finnhubIndustry %in% sect)))
X <- model.matrix(mm)
P <- X %*% solve(t(X) %*% X) %*% t(X)
mean(mm$residuals(1-diag(P)))
mean(mm$residuals/(1-diag(P)))
mm$residuals
mean((mm$residuals/(1-diag(P)))**2)
sqrt(mean((mm$residuals/(1-diag(P)))**2))
mean(df_nona$price)
plot(density(log(df_nona$pric)e))
plot(density(log(df_nona$price)))
mean(log(df_nona$price))
sd(log(df_nona$price))
mean((log(df_test$price) - pred_no_imp)**2)
sd(log(df_test$price))
summary(mm)
dir()
setwd("/Users/lucariotto/Documents/Personal/Gestione denaro/Company analysis/exploration/data")
write.csv(df_no_na, "/Users/lucariotto/Documents/Personal/Gestione denaro/Company analysis/exploration/data/train_non_na.csv", row.names = FALSE)
df = readr::read_csv("train_no_na.csv")
df_no_na
# use original data and drop NA rows
lambda = exp(seq(-10,5, length=100))
df_nona = readr::read_csv("train_non_na.csv")
# 5808 rows not na and 669 different company
id_df = data.frame(
cbind(df_nona[, 'ticker'] %>%  unique,
sample(1:5, length(df_nona[, 'ticker'] %>%  unique), replace = T))
)
colnames(id_df) = c('ticker', 'id')
id_df = df_nona['ticker'] |>
left_join(
id_df,
join_by(ticker)
)
X = df_nona |>
dplyr::select(-c(ticker, date_q, price))
model_cv = cv.glmnet(x = model.matrix(~ -1 + ., data = X),
y = log(df_nona[['price']]),
alpha = 0, lambda = lambda,
standardize = TRUE,
intercept = TRUE)
plot(model_cv)
model_cv$lambda.min
model_cv$cvm %>%  min
model = glmnet(x = model.matrix(~-1 + ., data = X),
y = log(df_nona[['price']]),
alpha = 0, lambda = model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE)
model$beta
# adaptive lasso
lambda = exp(seq(-10, 10, length= 300))
lasso_model_cv = cv.glmnet(x = model.matrix(~-1 + ., data = X),
y = log(df_nona[['price']]),
alpha = 1, lambda = lambda,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
plot(lasso_model_cv)
lasso_model_cv$lambda.min
lasso_model_cv$cvm %>%  min
lasso_model = glmnet(x = model.matrix(~-1 + ., data = X),
y = log(df_nona[['price']]),
alpha = 1, lambda = lasso_model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
lasso_model$beta
# check the the two model on test data
df_test = readr::read_csv("test_with_feats.csv")
library(boot)
# bootstrap estimate for confidence intervals
?boot
pb <- txtProgressBar(min = 0, max = 500, style = 3)
# bootstrap estimate for confidence intervals
lambda = exp(seq(-10, 10, length= 300))
N_df = nrow(df_nona)
boot_model = list()
pb <- txtProgressBar(min = 0, max = 500, style = 3)
for(i in 1:500)
{
boot_df_nona = df_nona[sample(1:N_df, length=N_df, replace = T), ]
X = boot_df_nona |>
dplyr::select(-c(ticker, date_q, price))
lasso_model_cv = cv.glmnet(x = model.matrix(~-1 + ., data = X),
y = log(boot_df_nona[['price']]),
alpha = 1, lambda = lambda,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
lasso_model = glmnet(x = model.matrix(~-1 + ., data = X),
y = log(boot_df_nona[['price']]),
alpha = 1, lambda = lasso_model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
boot_model[[i]] = lasso_model
setTxtProgressBar(pb, i)
}
?sample
pb <- txtProgressBar(min = 0, max = 500, style = 3)
for(i in 1:500)
{
boot_df_nona = df_nona[sample(1:N_df, size=N_df, replace = T), ]
X = boot_df_nona |>
dplyr::select(-c(ticker, date_q, price))
lasso_model_cv = cv.glmnet(x = model.matrix(~-1 + ., data = X),
y = log(boot_df_nona[['price']]),
alpha = 1, lambda = lambda,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
lasso_model = glmnet(x = model.matrix(~-1 + ., data = X),
y = log(boot_df_nona[['price']]),
alpha = 1, lambda = lasso_model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
boot_model[[i]] = lasso_model
setTxtProgressBar(pb, i)
}
# use original data and drop NA rows
lambda = exp(seq(-10,5, length=100))
# 5808 rows not na and 669 different company
id_df = data.frame(
cbind(df_nona[, 'ticker'] %>%  unique,
sample(1:5, length(df_nona[, 'ticker'] %>%  unique), replace = T))
)
colnames(id_df) = c('ticker', 'id')
id_df = df_nona['ticker'] |>
left_join(
id_df,
join_by(ticker)
)
X = df_nona |>
dplyr::select(-c(ticker, date_q, price))
model_cv = cv.glmnet(x = model.matrix(~ -1 + ., data = X),
y = log(df_nona[['price']]),
alpha = 0, lambda = lambda,
standardize = TRUE,
intercept = TRUE)
plot(model_cv)
model_cv$lambda.min
model_cv$cvm %>%  min
model = glmnet(x = model.matrix(~-1 + ., data = X),
y = log(df_nona[['price']]),
alpha = 0, lambda = model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE)
model$beta
# adaptive lasso
lambda = exp(seq(-10, 10, length= 300))
lasso_model_cv = cv.glmnet(x = model.matrix(~-1 + ., data = X),
y = log(df_nona[['price']]),
alpha = 1, lambda = lambda,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
plot(lasso_model_cv)
lasso_model_cv$lambda.min
lasso_model_cv$cvm %>%  min
lasso_model = glmnet(x = model.matrix(~-1 + ., data = X),
y = log(df_nona[['price']]),
alpha = 1, lambda = lasso_model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
lasso_model$beta
# bootstrap estimate for confidence intervals
lambda = exp(seq(-10, 10, length= 300))
N_df = nrow(df_nona)
boot_model = list()
pb <- txtProgressBar(min = 0, max = 500, style = 3)
for(i in 1:500)
{
boot_df_nona = df_nona[sample(1:N_df, size=N_df, replace = T), ]
X = boot_df_nona |>
dplyr::select(-c(ticker, date_q, price))
lasso_model_cv = cv.glmnet(x = model.matrix(~-1 + ., data = X),
y = log(boot_df_nona[['price']]),
alpha = 1, lambda = lambda,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
lasso_model = glmnet(x = model.matrix(~-1 + ., data = X),
y = log(boot_df_nona[['price']]),
alpha = 1, lambda = lasso_model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
boot_model[[i]] = lasso_model
setTxtProgressBar(pb, i)
}
pb <- txtProgressBar(min = 0, max = 500, style = 3)
for(i in 1:500)
{
boot_df_nona = df_nona[sample(1:N_df, size=N_df, replace = T), ]
boot_df_nona$finnhubIndustry = factor(boot_df_nona$finnhubIndustry, levels = unique(df_nona$finnhubIndustry))
X = boot_df_nona |>
dplyr::select(-c(ticker, date_q, price))
lasso_model_cv = cv.glmnet(x = model.matrix(~-1 + ., data = X),
y = log(boot_df_nona[['price']]),
alpha = 1, lambda = lambda,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
lasso_model = glmnet(x = model.matrix(~-1 + ., data = X),
y = log(boot_df_nona[['price']]),
alpha = 1, lambda = lasso_model_cv$lambda.min,
standardize = TRUE,
intercept = TRUE,
penalty.factor = 1/abs(model$beta))
boot_model[[i]] = lasso_model
setTxtProgressBar(pb, i)
}
setwd("/Users/lucariotto/Documents/Personal/Gestione denaro/Company analysis/target_data")
df_test = readr::read_csv("test_with_feats.csv")
df_test = df_test |>
distinct() |>
dplyr::select(colnames(df_nona))
df_test$finnhubIndustry = ifelse(is.na(df_test$finnhubIndustry), 'other', df_test$finnhubIndustry)
df_test = df_test %>%  na.omit
df_test$finnhubIndustry = factor(df_test$finnhubIndustry, levels = unique(df_nona$finnhubIndustry))
pred_matrix = matrix(NA, nrow = nrow(df_test), ncol = ncol(df_test))
X_test = df_test |> dplyr::select(-c(ticker, date_q, price))
X_test = model.matrix(~., X_test)
lapply(boot_model, function(m) predict(m, X_test))
do.call(cbind, lapply(boot_model, function(m) predict(m, X_test)))
do.call(cbind, lapply(boot_model, function(m) predict(m, X_test))) %>%  dim
pred_matrix = do.call(cbind, lapply(boot_model, function(m) predict(m, X_test))) %>%  dim
apply(pred_matrix, 1, function(x), quantile(x, c(0.025, 0.975)))
apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975)))
dim(pred_matrix)
pred_matrix = do.call(cbind, lapply(boot_model, function(m) predict(m, X_test))) %>%  dim
dim(pred_matrix)
pred_matrix = do.call(cbind, lapply(boot_model, function(m) predict(m, X_test)))
dim(pred_matrix)
apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975)))
apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975))) %>%  dim
apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975))) %>%  t
apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975))) %>%  t %>% exp
CI_test = apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975))) %>%  t %>% exp
test_perf = df_test |> dplyr::select(ticker, date_q, price)
test_perf[, 'pred', 'clow', 'cup'] = cbind(predict(lasso_model, X_test), CI_test)
cbind(predict(lasso_model, X_test), CI_test)
dim(cbind(predict(lasso_model, X_test), CI_test))
test_perf[, c('pred', 'clow', 'cup')] = cbind(predict(lasso_model, X_test), CI_test)
test_perf = test_perf |>
left_join(
test_perf |>
dplyr::select(ticker, date_q, price) |>
mutate(date_q = as.Date(date_q)  %m+% months(3)) |>
rename(c('price_tm1' = price)),
join_by(ticker, date_q)
)
test_perf
View(test_perf)
max(test_perf$price)
test_perf =
test_perf |>
mutate(is_between = between(price_tm1, clow, cup))
test_perf =
test_perf |>
mutate(is_between = !between(price_tm1, clow, cup))
test_perf$is_between |> sum
test_perf$is_between %>%  sum
test_perf$is_between %>%  sum(na.rm=T)
test_perf[which(test_perf), ]
test_perf[which(test_perf$is_between), ]
apply(pred_matrix, 2, function(x) quantile(x, c(0.025, 0.975))) %>%  t %>% exp %>% dim
CI_test = apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975))) %>%  t
test_perf = df_test |> dplyr::select(ticker, date_q, price)
test_perf[, c('pred', 'clow', 'cup')] = cbind(predict(lasso_model, X_test), CI_test)
test_perf = test_perf |>
left_join(
test_perf |>
dplyr::select(ticker, date_q, price) |>
mutate(date_q = as.Date(date_q)  %m+% months(3)) |>
rename(c('price_tm1' = price)),
join_by(ticker, date_q)
)
test_perf =
test_perf |>
mutate(is_between = !between(price_tm1, clow, cup))
test_perf$is_between %>%  sum(na.rm=T)
test_perf[which(test_perf$is_between), ]
pred_matrix = do.call(cbind, lapply(boot_model, function(m) exp(predict(m, X_test))))
CI_test = apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975))) %>%  t
test_perf = df_test |> dplyr::select(ticker, date_q, price)
test_perf[, c('pred', 'clow', 'cup')] = cbind(predict(lasso_model, X_test), CI_test)
test_perf = test_perf |>
left_join(
test_perf |>
dplyr::select(ticker, date_q, price) |>
mutate(date_q = as.Date(date_q)  %m+% months(3)) |>
rename(c('price_tm1' = price)),
join_by(ticker, date_q)
)
test_perf =
test_perf |>
mutate(is_not_between = !between(price_tm1, clow, cup))
test_perf$is_not_between %>%  sum(na.rm=T)
test_perf[which(test_perf$is_between), ]
test_perf[which(test_perf$is_not_between), ]
test_perf[, c('pred', 'clow', 'cup')] = cbind(exp(predict(lasso_model, X_test)), CI_test)
test_perf = test_perf |>
left_join(
test_perf |>
dplyr::select(ticker, date_q, price) |>
mutate(date_q = as.Date(date_q)  %m+% months(3)) |>
rename(c('price_tm1' = price)),
join_by(ticker, date_q)
)
test_perf =
test_perf |>
mutate(is_not_between = !between(price_tm1, clow, cup))
test_perf$is_not_between %>%  sum(na.rm=T)
test_perf[which(test_perf$is_not_between), ]
CI_test = apply(pred_matrix, 1, function(x) quantile(x, c(0.025, 0.975))) %>%  t
test_perf = df_test |> dplyr::select(ticker, date_q, price)
test_perf[, c('pred', 'clow', 'cup')] = cbind(exp(predict(lasso_model, X_test)), CI_test)
test_perf = test_perf |>
left_join(
test_perf |>
dplyr::select(ticker, date_q, price) |>
mutate(date_q = as.Date(date_q)  %m+% months(3)) |>
rename(c('price_tm1' = price)),
join_by(ticker, date_q)
)
test_perf =
test_perf |>
mutate(is_not_between = !between(price_tm1, clow, cup))
test_perf$is_not_between %>%  sum(na.rm=T)
test_perf[which(test_perf$is_not_between), ]
